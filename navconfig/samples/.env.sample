# NavConfig Environment File
# Copy this file to env/<environment>/.env and adjust values.

# Environment name (dev, staging, prod)
ENV=dev

# Debug mode (true/false)
DEBUG=true

# Path to the INI configuration file (relative to project root)
CONFIG_FILE=etc/config.ini

# Application name
APP_NAME=MyApp

# Production mode (true/false)
PRODUCTION=false

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOGLEVEL=DEBUG
