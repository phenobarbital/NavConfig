# NavConfig -- Cache configuration.
# NavConfig can read configuration values from Redis as an external reader.

# Cache backend: "redis", or empty to disable the cache reader entirely.
CACHE_BACKEND=

# -- Redis --
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1
