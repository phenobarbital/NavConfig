# NavConfig -- Resource configuration.
# Loaded after ".env". Keep non-secret, resource-level directives here.

# Directory served for static assets (relative to the project root).
STATIC_DIR=static

# Directory where user uploads are stored.
UPLOAD_DIR=uploads

# Directory used for temporary files.
TEMP_DIR=/tmp

# Directory where templates are read from.
TEMPLATE_DIR=templates
