# NavConfig -- Local overrides.
# This file is loaded last, so anything defined here wins over every other
# .env file of the environment. Keep it out of version control.

# DEBUG=true
# LOGLEVEL=DEBUG
